import java.util.*;

public class EmployeeFunction {

    // Sample Function/Method
    public static int calculateSalary(int salary, int workHours) {
        return salary * workHours;
    }

    public static void main(String[] args) {
        /*
         *  Build Dashboard (Employee - me, Admin - Rejie) 
         *  1. View employee details (View employee number, name, birthday)
         *  2. See weekly salary based on hours worked
         *  3. Calculate weekly wage based on hours worked and hourly rate (with deductions)
         */

        // Initialise Scanner
        Scanner s = new Scanner(System.in);

        // Initialise control for loop
        int option = -1;

        // Loop for menu
        while (option != 4) {
            // Print all menu options
            System.out.println("----------EMPLOYEE MENU----------");
            System.out.println("1. View employee details");
            System.out.println("2. See weekly salary");
            System.out.println("3. Calculate weekly salary with deductions");
            System.out.println("4. Exit menu");
            System.out.printf("Choose an option > ");
            try {
                option = s.nextInt();
                switch (option) {
                    case 1:
                        // TODO: Case 1 Function
                        // View others: need to prompt user for employee id/name to search (For Admin)
                        // View self: just search based on previous user input using id or name
                        break;
                    case 2:
                        // TODO: Case 2 Function
                        // Calling sample function/method and printing results
                        break;
                    case 3:
                        // TODO: case 3 function
                        // Should user prompt for hourly wage and hours worked?
                        // What to do with deductions? Prompted?
                        break;
                    case 4:
                        // TODO: case 4 Exit
                        // Exit message
                        break;
                    default:
                        // TODO: Default incorrect input message
                }
            } catch (Exception e) {
                // TODO: edit to proper error message if necessary
                System.out.println("Placeholder error message");
                //System.out.println(e.getMessage());
                //e.printStackTrace();
                s.nextLine();
            }
        }
    }
}
